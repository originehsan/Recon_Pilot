# Copy this file to .env and fill in real values. Never commit .env.

# MySQL connection
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=settlement_recon

# Google Gemini API key (used by the AI investigation stage)
GEMINI_API_KEY= gemini_api_key
